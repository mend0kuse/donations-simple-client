import { Layout } from './layout';

export const MainPage = () => {
    return <Layout>main</Layout>;
};

// TODO

// list of donations + add index to donation manager getters  
// transition to donation page with details
// add/remove admins
// enable/disable admins
// create donations
// change settings