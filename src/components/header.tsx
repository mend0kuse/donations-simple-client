import { TonConnectButton } from '@tonconnect/ui-react';
import './header.css';

export const Header = () => {
    return (
        <header className='header'>
            <TonConnectButton />
        </header>
    );
};
